spring:
  application:
    name: ai-blog
  datasource:
    url: jdbc:mysql://localhost:3306/aiblog?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: aiblog
    password: aiblog123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
server:
  port: 8080

logging:
  level:
    com.aiblog: DEBUG